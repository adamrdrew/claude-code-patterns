# Active Memory Index

This index tracks all active memory entries.

---

## Entries

*No entries yet. Add memory entries to this directory.*

---

## Memory Guidelines

- Each entry should include metadata (created, last-used, context, expires)
- Use `Expires: never` for permanent entries
- Run `review-relevance` periodically to assess staleness
- Prune obsolete entries to keep memory lean
